{
  "name": "ipo-family-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/prepare-data.mjs && next dev",
    "build": "node scripts/prepare-data.mjs && next build && node scripts/prepare-sw.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:data": "python -m unittest discover -s tests -p test_*.py"
  },
  "dependencies": {"next": "^15.5.12", "react": "^19.1.0", "react-dom": "^19.1.0"},
  "devDependencies": {"@types/node": "^22", "@types/react": "^19", "@types/react-dom": "^19", "typescript": "^5.9", "eslint": "^9", "eslint-config-next": "^15.5.12", "vitest": "^3.2.0", "jsdom": "^26.1.0", "@testing-library/react": "^16.3.0"}
}
